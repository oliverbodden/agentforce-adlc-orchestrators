# JIRA Ticket: GeneralFAQ — Compact topic instructions to ~60% without quality regression

> Copy into a new PROJECT ticket.

---

**Title:** GeneralFAQ — Compact topic instructions to ~60% without quality regression

**Type:** Story

**Labels:** `prompt-optimization`, `example-service-agent`, `general-faq`

---

## Context

The `GeneralFAQ` topic instruction for Example Service Agent is currently ~2,194 words (across 2 instruction records). This is a RAG-based topic that answers customer questions using internal knowledge articles. It does not call account-specific APIs and does not require a routable ID for testing.

**Agent:** Example Service Agent (`Example_Service_Agent`)
**Org:** sandbox-alias (my-sandbox)
**Topic:** `GeneralFAQ` (`GeneralFAQ_16jcf8c53e60308`, ID: `179Em000000BGsiIAG`)
**Instruction source of truth:** unknown until `adlc-drive` verifies current org metadata; if no usable authoring bundle exists, use the local UI-built Tooling API exception path.
**Instruction records:**

- `1C7Em0000006yXnKAI` — Main instruction (1,679 words)
- `1C7Em0000006yXoKAI` — Voice and tone (515 words)

**Current total:** ~2,194 words
**Target:** ~1,316 words (60% of original)
**Requested staging:** Stage 1 compaction and quality preservation. Stage 2 style/tone polish only if explicitly approved after Stage 1 acceptance.

## Requirements

1. Compact the topic instructions to ~60% of original word count
2. Preserve all decision logic and reasoning scaffolding (Store: directives, step checkpoints)
3. Preserve voice and tone guidelines (may be compactable but core tone must hold)
4. Preserve knowledge retrieval patterns and response formatting
5. Run full eval and compare against baseline

## Acceptance Criteria

- Overall eval results improve or hold vs baseline
- No individual metric regresses more than 10%
- Cross-topic regression check: other topics not affected
- Response quality (answer accuracy, relevance) >= baseline
- URL consistency maintained or improved

## ADLC Acceptance State

`adlc-drive` will create `config.json` in Phase 3d after a fresh baseline run. Criteria start as `proposed` and become authoritative after the Phase 3 checkpoint is approved.

Suggested criteria to convert into `config.json`:

- `ticket_goal`: compact GeneralFAQ instructions to the approved target size, priority `critical`, tolerance `exact` or explicit target, blocking `true`, stage `stage_1`
- `regression`: no individual metric regresses more than 10%, priority `critical`, tolerance `standard` with `max_regression_delta`, blocking `true`, stage `stage_1`
- `product_acceptance`: FAQ answer accuracy, relevance, URL consistency, and tone hold on representative scenarios, priority `high`, tolerance `manual_review`, blocking based on product risk, stage `stage_1`
- Future style/tone polish remains `deferred` unless the user explicitly promotes the run to Stage 2.

## Eval Baseline

- **Baseline CSV:** Attached — `Example FAQ Redesign Baseline`
- **Prior eval report (optional):** Attached — `Eval Analysis: 2.19.1 vs 2.15.1 vs 2.9.1` (for context — URL consistency was improved in a later version)
- **Baseline location:** `adlc/agents/[agent-dev-name]__[org-alias]/baselines/general-faq/` (to be created by adlc-drive)

> Note: The regression eval report will be auto-generated by `adlc-drive` during Phase 6. The regression script will need to be adapted for FAQ-specific metrics (not Invoice metrics).

## Topic-Specific Notes

- **This is a RAG topic** — it retrieves answers from knowledge articles, not account/invoice data
- **No routable ID needed** — no account-specific API calls
- **Key quality metrics for FAQ:** answer accuracy, knowledge retrieval relevance, URL correctness, response completeness, tone adherence
- **Not applicable from Invoice:** IDENTIFY/EXPLAIN strategies, invoice matching, auto-selection, escalation rules
- **The regression script (`generate_report.py`) is topic-agnostic** — it discovers metrics from data (strategy distribution, formatting, length, consistency). FAQ-specific criteria (answer accuracy, URL correctness) are handled by the AI in Phase 6 using the JSON sidecar output.
- **Diagnostic mode caveat:** temporary user-facing diagnostic traces may be used only with HITL approval and must be removed/internalized, or explicitly product-approved to remain, before final `GO`.

## Approach

Uses `adlc-drive` skill with surgical compaction (per learnings from PROJ-345 Invoice optimization).

1. **Goal:** Pull this ticket via JIRA integration
2. **Refine:** Confirm topic scope, instruction records, baseline reference
3. **Discover:** Resolve canonical metadata/source of truth, pull current instructions (both records), analyze structure, run baseline tests, define FAQ-specific eval metrics, and create `discovery.json` plus proposed `config.json`
4. **Plan:** Surgical compaction strategy — preserve reasoning flow, compress prose, and keep style polish staged unless approved
5. **Execute:** Iterative loop — compact → test → evaluate → iterate against current-stage blocking criteria
6. **Present:** Eval report with before/after comparison and per-criterion `config.json` results
7. **Hand off:** Deploy/publish via `developing-agentforce` or leave in sandbox

## Key Learnings from Prior Optimizations

- **Never do a full rewrite** — removing Store: blocks and step checkpoints causes the LLM to shortcut to default/fallback behaviors
- **Surgical approach works:** cut separators, duplicate rules, verbose prose. Keep all reasoning flow intact
- **HTML encoding in Testing Center output** — use `html.unescape()` in scoring scripts
- **Early directive sections are high-impact** — the LLM reads these first and forms its mental model
- **Eval metrics are topic-specific** — discover what matters for this topic before defining acceptance criteria
- **Account data doesn't affect this topic** — GeneralFAQ is RAG-only, results should be stable across runs

## Definition of Done

- Instructions compacted to target word count (~1,316 words)
- Full eval report generated (HTML + JSON where possible)
- `discovery.json` records source of truth, canonical folder, instruction IDs, baseline CSV, config path, suite name, and diagnostic mode if used
- `config.json` records approved acceptance criteria, thresholds, stages, and Phase 6 results
- Eval results within current-stage acceptance criteria
- FAQ-specific criteria evaluated by Phase 6 analysis using the topic-agnostic JSON sidecar and representative examples
- Any temporary diagnostic traces removed/internalized or explicitly product-approved to remain
- Instruction deployed to target org
- Eval artifacts saved locally and submitted to the shared ADLC artifact repo when closing out: `https://github.com/YOUR_ORG/YOUR_ADLC_ARTIFACT_REPO`
