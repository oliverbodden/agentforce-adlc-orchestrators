# JIRA Ticket: GeneralFAQ — Compact topic instructions to ~60% without quality regression

> Copy into a new ESCHAT ticket.

---

**Title:** GeneralFAQ — Compact topic instructions to ~60% without quality regression

**Type:** Story

**Labels:** `prompt-optimization`, `indeed-service-agent`, `general-faq`

---

## Context

The `GeneralFAQ` topic instruction for Indeed Service Agent is currently ~2,194 words (across 2 instruction records). This is a RAG-based topic that answers customer questions using internal knowledge articles. It does not call account-specific APIs and does not require a routable ID for testing.

**Agent:** Indeed Service Agent (`Agentforce_Service_Agent`)
**Org:** devesa3 (my-sandbox)
**Topic:** `GeneralFAQ` (`GeneralFAQ_16jcf8c53e60308`, ID: `179Em000000BGsiIAG`)
**Instruction records:**

- `1C7Em0000006yXnKAI` — Main instruction (1,679 words)
- `1C7Em0000006yXoKAI` — Voice and tone (515 words)

**Current total:** ~2,194 words
**Target:** ~1,316 words (60% of original)

## Requirements

1. Compact the topic instructions to ~60% of original word count
2. Preserve all decision logic and reasoning scaffolding (Store: directives, step checkpoints)
3. Preserve voice and tone guidelines (may be compactable but core tone must hold)
4. Preserve knowledge retrieval patterns and response formatting
5. Run full eval and compare against baseline

## Acceptance Criteria

- Overall eval results improve or hold vs baseline
- No individual metric regresses more than 10%
- Cross-topic regression check: other topics not affected
- Response quality (answer accuracy, relevance) >= baseline
- URL consistency maintained or improved

## Eval Baseline

- **Baseline CSV:** Attached — `ESA - FAQ Redesign - 3 Step Approach - QA 2.24.1`
- **Prior eval report (optional):** Attached — `Eval Analysis: 2.19.1 vs 2.15.1 vs 2.9.1` (for context — URL consistency was improved in a later version)
- **Baseline location:** `evals/indeed-service-agent/baselines/[version]/` (to be created by adlc-drive)

> Note: The regression eval report will be auto-generated by `adlc-drive` during Phase 6. The regression script will need to be adapted for FAQ-specific metrics (not Invoice metrics).

## Topic-Specific Notes

- **This is a RAG topic** — it retrieves answers from knowledge articles, not account/invoice data
- **No routable ID needed** — no account-specific API calls
- **Key quality metrics for FAQ:** answer accuracy, knowledge retrieval relevance, URL correctness, response completeness, tone adherence
- **Not applicable from Invoice:** IDENTIFY/EXPLAIN strategies, invoice matching, auto-selection, escalation rules
- **The regression script (`run_regression.py`) was built for Invoice** — it will need new metric definitions for FAQ. adlc-drive should discover appropriate metrics during Phase 3

## Approach

Uses `adlc-drive` skill with surgical compaction (per learnings from PROJ-345 Invoice optimization).

1. **Goal:** Pull this ticket via JIRA integration
2. **Refine:** Confirm topic scope, instruction records, baseline reference
3. **Discover:** Pull current instructions (both records), analyze structure, run baseline tests, define FAQ-specific eval metrics
4. **Plan:** Surgical compaction strategy — preserve reasoning flow, compress prose
5. **Execute:** Iterative loop — compact → test → evaluate → iterate
6. **Present:** Eval report with before/after comparison
7. **Hand off:** Deploy via `adlc-deploy` or leave in sandbox

## Key Learnings from Prior Optimizations

- **Never do a full rewrite** — removing Store: blocks and step checkpoints causes the LLM to shortcut to default/fallback behaviors
- **Surgical approach works:** cut separators, duplicate rules, verbose prose. Keep all reasoning flow intact
- **HTML encoding in Testing Center output** — use `html.unescape()` in scoring scripts
- **Early directive sections are high-impact** — the LLM reads these first and forms its mental model
- **Eval metrics are topic-specific** — discover what matters for this topic before defining acceptance criteria
- **Account data doesn't affect this topic** — GeneralFAQ is RAG-only, results should be stable across runs

## Definition of Done

- Instructions compacted to target word count (~1,316 words)
- Full eval report generated (HTML)
- Eval results within acceptance criteria
- FAQ-specific regression script created or adapted
- Instruction deployed to target org
- Eval artifacts saved in `evals/indeed-service-agent/tickets/[ticket-key]/`

